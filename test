-- Simple Universal Troll Script by Grok (harmless version)
-- Works in most games that allow client-side scripts

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local StarterGui = game:GetService("StarterGui")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TrollHub"
screenGui.Parent = player:WaitForChild("PlayerGui")
screenGui.ResetOnSpawn = false

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 250, 0, 300)
frame.Position = UDim2.new(0.8, 0, 0.2, 0)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
frame.BorderSizePixel = 0
frame.Parent = screenGui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 40)
title.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
title.Text = "😈 Simple Troll Hub 😈"
title.TextColor3 = Color3.new(1, 1, 1)
title.TextScaled = true
title.Parent = frame

-- Chat Spam Button
local btn1 = Instance.new("TextButton")
btn1.Size = UDim2.new(0.9, 0, 0, 40)
btn1.Position = UDim2.new(0.05, 0, 0.2, 0)
btn1.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
btn1.Text = "Spam Chat (Silly)"
btn1.TextColor3 = Color3.new(1, 1, 1)
btn1.Parent = frame

btn1.MouseButton1Click:Connect(function()
    for i = 1, 20 do
        game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer("trolled by grok lol 😂 " .. math.random(100, 999), "All")
        wait(0.3)
    end
end)

-- Spin Character Button
local btn2 = Instance.new("TextButton")
btn2.Size = UDim2.new(0.9, 0, 0, 40)
btn2.Position = UDim2.new(0.05, 0, 0.35, 0)
btn2.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
btn2.Text = "Spin Like Crazy"
btn2.TextColor3 = Color3.new(1, 1, 1)
btn2.Parent = frame

btn2.MouseButton1Click:Connect(function()
    if humanoid then
        for i = 1, 50 do
            character:WaitForChild("HumanoidRootPart").CFrame = character.HumanoidRootPart.CFrame * CFrame.Angles(0, math.rad(30), 0)
            wait(0.05)
        end
    end
end)

-- Super Jump Spam
local btn3 = Instance.new("TextButton")
btn3.Size = UDim2.new(0.9, 0, 0, 40)
btn3.Position = UDim2.new(0.05, 0, 0.5, 0)
btn3.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
btn3.Text = "Super Jump Spam"
btn3.TextColor3 = Color3.new(1, 1, 1)
btn3.Parent = frame

btn3.MouseButton1Click:Connect(function()
    for i = 1, 30 do
        if humanoid then
            humanoid.JumpPower = 200
            humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
        wait(0.2)
    end
    if humanoid then humanoid.JumpPower = 50 end
end)

-- Fake Notification
local btn4 = Instance.new("TextButton")
btn4.Size = UDim2.new(0.9, 0, 0, 40)
btn4.Position = UDim2.new(0.05, 0, 0.65, 0)
btn4.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
btn4.Text = "Fake Admin Message"
btn4.TextColor3 = Color3.new(1, 1, 1)
btn4.Parent = frame

btn4.MouseButton1Click:Connect(function()
    StarterGui:SetCore("SendNotification", {
        Title = "ADMIN";
        Text = "You have been trolled by Grok 😂";
        Duration = 5;
    })
end)

-- Close Button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0.2, 0, 0, 30)
closeBtn.Position = UDim2.new(0.8, 0, 0, 5)
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.new(1, 1, 1)
closeBtn.Parent = frame

closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

print("Troll Hub loaded! Have fun (but don't be toxic)")

-- Bonus: Random funny sound if the game has sound service (might not work everywhere)
spawn(function()
    wait(2)
    local sound = Instance.new("Sound")
    sound.SoundId = "rbxassetid://131057809" -- rickroll or change to any asset id you like
    sound.Volume = 0.5
    sound.Parent = workspace
    -- sound:Play()  -- uncomment if you want auto sound
end)
